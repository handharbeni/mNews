<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="illiyin.mhandharbeni.servicemodule">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" />
    <permission-group
        android:name="android.permission-group.LOCATION" />
    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">
        <service android:name=".service.MainService"/>
        <service android:name=".service.intentservice.ContactService"/>
        <service android:name=".service.intentservice.ChatService"/>
        <service android:name=".service.intentservice.GrupService"/>
        <service android:name=".service.intentservice.ListMemberService"/>
        <service android:name=".service.intentservice.ListMemberLocation"/>
        <service android:name=".service.intentservice.GroupLocationService"/>
    </application>
</manifest>
